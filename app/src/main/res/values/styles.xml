<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <item name="android:actionMenuTextColor">@color/colorWhite</item>
        <item name="actionMenuTextColor">@color/colorWhite</item>

        <item name="android:windowTranslucentStatus">true</item>

        <!--<item name="android:windowDrawsSystemBarBackgrounds">true</item>-->
        <!--<item name="android:statusBarColor">@color/colorPrimaryTransparent</item>-->
    </style>

    <style name="AppTheme.Toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/colorWhite</item>
    </style>

    <style name="AppTheme.Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!-- Set proper title size -->
        <item name="android:textSize">@dimen/toolbar_text_material_size</item>
        <!-- Set title color -->
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="spinnerItemStyle">
        <item name="android:textAppearance">@style/textAppearanceSpinnerItem</item>
        <!--<item name="android:padding">10sp</item>-->
    </style>

    <style name="textAppearanceSpinnerItem" parent="android:TextAppearance.Holo.Widget.TextView.SpinnerItem">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="textViewStyle" parent="@android:style/Widget.TextView">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorGray</item>
    </style>

</resources>
